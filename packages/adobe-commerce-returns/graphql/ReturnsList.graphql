query ReturnsList($currentPage: Int = 1, $pageSize: Int = 20) {
  customer {
    returns(currentPage: $currentPage, pageSize: $pageSize) {
      total_count
      page_info {
        current_page
        page_size
        total_pages
      }
      items {
        ...Return_Overview
      }
    }
  }
}
